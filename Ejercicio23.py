# Crear una función buscar que mediante una expresion regular indique si una palabra está ebn una frase
import re
def buscar (frase):
    frase = 'Esto es una frase para buscar'
    re.search(frase)
    
buscar('frase')
    